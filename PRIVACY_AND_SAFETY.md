# 匿名性・安全・公開リポジトリ運用

## 公開リポジトリは作業場にしない

このリポジトリには、検査済みの公開物だけを投入します。

置かないもの：

- 未公開・レビュー前の原稿
- AIとの会話・作業記録
- 実行ログ、デバッグ出力、ローカル環境情報
- 個人メール、個人名、端末名、ローカルパス
- 回収前のレビューZIP
- 非公開資料・個人情報

## リリース手順

1. 非公開ローカル領域で制作する。
2. 公開用ディレクトリへクリーンコピーする。
3. note貼り付け本文、画像、リポジトリ、Release候補を同じ監査ゲートに通す。
4. 監査レポートはリポジトリ外へ出力する。
5. `git status --short`、`git diff --cached --check`、`git diff --cached`を確認する。
6. `git log --format='%h %an <%ae>' --all`でauthor名とメールを確認する。
7. 検査にPASSしたファイルだけをコミットする。
8. タグとGitHub Releaseを作成する。
9. バイナリ成果物は原則としてReleaseへ添付する。

監査は、禁止パターン、ローカルパス、Office・PDFメタデータ、画像EXIF・PNGテキスト情報、ZIP内部をfail-closedで検査します。監査不能な画像・PDFはPASSにしません。

## Git履歴の注意

公開後にファイルを削除しても、過去コミットやクローン等に残る可能性があります。機微情報を誤って公開した場合は、単なる削除ではなく、認証情報の失効、履歴書換え、GitHubへの削除依頼等が必要になる場合があります。

## アカウント

- 専用メール、専用ユーザー名、専用アイコン
- リポジトリローカルの`user.name`を企画名へ固定
- コミットはGitHubの`noreply`メール
- 個人メールを含むpushのブロック
- 二要素認証またはパスキー、復旧コードの安全な保管
- 個人アカウントとの相互リンクを避ける

## noteコメント・メッセージ

無料noteで開始するため、コメント欄は受付状態です。ただし初期30日は原則として返信しません。個人情報、脅迫、差別、皇族本人の内心推測、執拗な個人攻撃は削除・非表示・通報の対象とします。メッセージは受信対象の全項目をOFFにします。
